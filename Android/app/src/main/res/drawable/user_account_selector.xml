<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when pressed -->
    <item android:drawable="@drawable/user_black" android:state_pressed="true" />
    <!-- State when selected -->
    <item android:drawable="@drawable/user_black" android:state_selected="true" />
    <!-- Default state -->
    <item android:drawable="@drawable/user_account" />
</selector>
